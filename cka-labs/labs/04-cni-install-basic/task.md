Scenario #4
Install and configure a CNI of your choice:
- Flannel v0.26.1 (manifest URL in task)
OR
- Calico v3.28.2 (manifest URL in task)
Install from manifest files (no Helm).

Validate:
- Nodes Ready
- CNI pods running in kube-system
